# Maintenance scripts

This directory contains project and dataset maintenance commands that are not
part of the importable `chemflow` runtime package.

- `check_status.py` inspects training-job status files.
- `combine_biogen_expansionrx_clearance.py` builds the combined clearance data.
- `combine_hlm_datasets.py` builds the combined HLM data.
